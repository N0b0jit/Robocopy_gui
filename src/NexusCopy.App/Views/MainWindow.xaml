<Window x:Class="NexusCopy.App.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:NexusCopy.App.ViewModels"
        xmlns:views="clr-namespace:NexusCopy.App.Views"
        mc:Ignorable="d"
        Title="{Binding WindowTitle}"
        Height="700" 
        Width="900"
        MinHeight="600"
        MinWidth="800"
        WindowStartupLocation="CenterScreen"
        Background="{DynamicResource NexusBackgroundBrush}">
    
    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/Styles/Colors.xaml"/>
                <ResourceDictionary Source="/Styles/Typography.xaml"/>
                <ResourceDictionary Source="/Styles/Controls.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>
    
    <Window.DataContext>
        <vm:MainViewModel/>
    </Window.DataContext>
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <!-- Header -->
        <Border Grid.Row="0"
                Background="{StaticResource NexusPrimaryBrush}"
                Padding="16,12">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                
                <StackPanel Grid.Column="0" Orientation="Horizontal">
                    <TextBlock Text="🚀" 
                               FontSize="24" 
                               VerticalAlignment="Center"
                               Margin="0,0,12,0"/>
                    <TextBlock Text="Nexus Copy" 
                               Style="{StaticResource NexusTitleStyle}"
                               VerticalAlignment="Center"
                               Foreground="White"/>
                </StackPanel>
                
                <StackPanel Grid.Column="1" 
                            Orientation="Horizontal"
                            VerticalAlignment="Center">
                    <Button Content="About"
                            Command="{Binding ShowAboutCommand}"
                            Style="{StaticResource NexusSecondaryButtonStyle}"
                            Background="Transparent"
                            BorderBrush="White"
                            Foreground="White"
                            Margin="0,0,8,0"
                            Padding="12,6"/>
                    <Button Content="Exit"
                            Command="{Binding ExitCommand}"
                            Style="{StaticResource NexusSecondaryButtonStyle}"
                            Background="Transparent"
                            BorderBrush="White"
                            Foreground="White"
                            Padding="12,6"/>
                </StackPanel>
            </Grid>
        </Border>
        
        <!-- Tab Content -->
        <TabControl Grid.Row="1"
                    SelectedIndex="{Binding SelectedTabIndex}"
                    Background="{DynamicResource NexusBackgroundBrush}"
                    BorderThickness="0">
            
            <!-- Copy Job Tab -->
            <TabItem Header="Copy Job">
                <views:CopyJobView/>
            </TabItem>
            
            <!-- History Tab -->
            <TabItem Header="History">
                <views:HistoryView/>
            </TabItem>
            
            <!-- Profiles Tab -->
            <TabItem Header="Profiles">
                <views:ProfileManagerView/>
            </TabItem>
            
            <!-- Settings Tab -->
            <TabItem Header="Settings">
                <views:SettingsView/>
            </TabItem>
        </TabControl>
        
        <!-- Status Bar -->
        <StatusBar Grid.Row="2"
                   Background="{DynamicResource NexusSurfaceBrush}"
                   BorderThickness="0,1,0,0"
                   BorderBrush="{DynamicResource NexusBorderBrush}">
            <StatusBarItem>
                <TextBlock Text="{Binding StatusMessage}"
                           Style="{StaticResource NexusBodyStyle}"/>
            </StatusBarItem>
        </StatusBar>
    </Grid>
</Window>
