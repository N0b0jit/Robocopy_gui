<UserControl x:Class="NexusCopy.App.Controls.LogViewerControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="200" d:DesignWidth="600">
    
    <Border BorderThickness="1" 
            BorderBrush="{DynamicResource NexusBorderBrush}"
            CornerRadius="4"
            Background="{DynamicResource NexusSurfaceBrush}">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            
            <!-- Header -->
            <Border Grid.Row="0"
                    Background="{DynamicResource NexusPrimaryBrush}"
                    Padding="8,4"
                    CornerRadius="4,4,0,0">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    
                    <TextBlock Grid.Column="0"
                               Text="LOG OUTPUT"
                               Style="{StaticResource NexusBodyStyle}"
                               Foreground="White"
                               FontWeight="SemiBold"/>
                    
                    <Button Grid.Column="1"
                            Content="Clear"
                            Command="{Binding ClearLogCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                            Style="{StaticResource NexusSecondaryButtonStyle}"
                            Padding="8,4"
                            Background="Transparent"
                            BorderBrush="White"
                            Foreground="White"/>
                </Grid>
            </Border>
            
            <!-- Log Content -->
            <ScrollViewer Grid.Row="1"
                          x:Name="LogScrollViewer"
                          VerticalScrollBarVisibility="Auto"
                          HorizontalScrollBarVisibility="Auto"
                          Margin="8">
                <TextBox x:Name="LogTextBox"
                         Text="{Binding LogOutput, RelativeSource={RelativeSource AncestorType=UserControl}, Mode=OneWay}"
                         IsReadOnly="True"
                         Background="Transparent"
                         BorderThickness="0"
                         TextWrapping="NoWrap"
                         FontFamily="{StaticResource NexusMonospaceFontFamily}"
                         FontSize="11"
                         VerticalAlignment="Top"
                         HorizontalAlignment="Left"
                         Padding="0"/>
            </ScrollViewer>
        </Grid>
    </Border>
</UserControl>
